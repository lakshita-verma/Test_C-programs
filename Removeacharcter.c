#include<stdio.h>
#include<string.h>
int  main(
    char str[50],strlen;
    int i,j;
    printf("Enter the string:");
    gets(str);
    printf("Enter the character to be replaced:");
    scanf("%d",&str);
    for (i=0;i<len;i++)
    {
        if strlen[i]==o
        {
            for(j=1;j<strlen;j+=+)
            {
                str[j]=str[j=1];
            }
            len--;
            i--;
        }
    }
    printf("Removal of string '%c'=%s",ch,str);
    return 0;
)